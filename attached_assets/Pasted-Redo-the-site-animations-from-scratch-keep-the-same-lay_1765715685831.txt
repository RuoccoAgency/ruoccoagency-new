Redo the site animations from scratch (keep the same layout/content/colors).
The current result is not impressive. I want a “WOW” 3D experience.

Non-negotiable

Do NOT change copy/text, section order, or page structure.

Keep the same logo gradient colors already in the site.

Only change animations + effects.

WOW 3D Animation Concept (must implement)
1) Global “Depth” System

Add a global 3D scene feeling using CSS perspective:

perspective: 1200px on the main wrapper

sections/cards can move on Z axis with translateZ()

Add subtle camera parallax following mouse (desktop only):

entire page background moves slightly (2–6px)

foreground cards move more (6–14px)

use requestAnimationFrame for smoothness

2) Hero: 3D Hologram Reveal

On load: hero elements appear with 3D rotation + depth:

title comes in from rotateX(18deg) translateZ(60px)

subtitle from rotateX(12deg) translateZ(40px)

CTA button with a neon sweep and glow pulse

Add a holographic shimmer over the title (masked gradient moving slowly).

3) Cards: Real 3D “Glass + Neon Edge” Tilt

Replace any existing tilt with a premium system:

On hover: cards tilt based on cursor position with:

rotateX, rotateY, translateZ(18px)

add specular highlight that moves with cursor (pseudo-element overlay)

Add neon edge (animated gradient border) that intensifies on hover.

Add a floating idle animation (very subtle) even without hover.

4) Scroll “Portal” Transitions Between Sections

When entering each section:

Create a short portal-like transition:

section fades in + rotates slightly in 3D + slides from depth

background particles briefly brighten then settle
Use Framer Motion useScroll + useTransform:

Each section has a unique animation timing, not copy-paste.

5) Background: Animated Starfield + Nebula Depth

Keep black background but make it cinematic:

layered starfield: 3 layers moving at different speeds (parallax)

faint nebula gradient blobs slowly shifting

occasional soft twinkle (random opacity)
No heavy libraries—do it with canvas OR pure CSS layers (choose the smoothest).

6) “How it works” Timeline: 3D Growth Line (WOW)

The vertical line draws as you scroll down (like liquid neon).

It must be glowing, with a subtle animated energy traveling upward.

Once the line reaches the end, it stays complete and does not rewind when scrolling up.
Implementation requirement:

compute progress using scroll position and clamp to max;

store a hasCompleted boolean so it never resets.

The line and nodes should have a neon bloom.

7) Add This Badge Under “Chi Sono”

Under the “Chi Sono” title, add a pill badge identical to the existing “AI Automation Agency” pill style:

text: “Fondatore di RuoccoAgency”

same styling, gradient glow, and subtle float.

Motion Quality Rules

Use spring animations (no stiff easing).

Add staggered reveals, but avoid overdoing: premium and smooth.

Mobile: reduce effects intensity, keep wow but lightweight.

Support prefers-reduced-motion.

Deliverable

Implement all changes in code (React + Tailwind + Framer Motion).
Do not change content—only animations/effects + the new badge under “Chi Sono”.